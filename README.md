# forTrace
A simple drawing tool. I created this to make adjustments to bitmap images generated by mmTool’s edge detection feature before passing them to a vector editing app. As an additional feature, I’ve implemented pose detection for character images.
